<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <uses-sdk
        android:minSdkVersion="28"
        android:targetSdkVersion="34" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.trindade.preferences.screen.App"
        android:theme="@style/TrindadeTheme">
        <activity
            android:name="com.trindade.preferences.screen.MainAct"
            android:exported="true">
            <intent-filter>
                <action 
                    android:name="android.intent.action.MAIN" />
                <category 
                    android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity 
            android:name="com.trindade.preferences.screen.SettingsAct" />
        <activity 
            android:name="com.trindade.preferences.screen.DebugActivity" />
    </application>
</manifest>
